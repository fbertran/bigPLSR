% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plsR1.R
\name{pls1_dense_ya}
\alias{pls1_dense_ya}
\alias{pls1_stream_ya}
\title{Single-response partial least squares regression (PLS1) yet another implementation}
\usage{
pls1_dense_ya(x, y, ncomp, tol = 1e-08, algorithm = c("simpls", "nipals"))

pls1_stream_ya(
  x,
  y,
  ncomp,
  chunk_size = 4096,
  tol = 1e-08,
  algorithm = c("simpls", "nipals")
)
}
\arguments{
\item{x, y}{Predictor and response objects stored as double precision
\code{\link[bigmemory:big.matrix]{bigmemory::big.matrix}} instances. The response must contain a single
column. The dense helper also accepts numeric vectors for \code{y} and converts
them transparently. The dense routine copies the predictors into an R
matrix, while the streaming version accesses them in blocks.}

\item{ncomp}{Number of latent components to extract.}

\item{tol}{Convergence tolerance used when estimating each component. Only
relevant for the dense variant.}

\item{algorithm}{Algorithm used to compute the PLS fit. Either "simpls" or
"nipals". The SIMPLS backend is generally faster when the data fits in
memory.}

\item{chunk_size}{Number of rows processed per block by the streaming
variant.}
}
\value{
A list containing regression coefficients, intercept, loadings and
preprocessing statistics. The structure matches the output of the
underlying C++ routines.
}
\description{
Single-response partial least squares regression (PLS1) yet another implementation
}
\examples{
\donttest{
library(bigmemory)
X <- as.big.matrix(matrix(rnorm(2000), nrow = 100))
y <- as.big.matrix(matrix(rnorm(100), ncol = 1))
fit <- pls1_dense_ya(X, y, ncomp = 3)
str(fit)
}

\donttest{
library(bigmemory)
X <- as.big.matrix(matrix(rnorm(2000), nrow = 100))
y <- as.big.matrix(matrix(rnorm(100), ncol = 1))
fit <- pls1_stream_ya(X, y, ncomp = 3)
str(fit)
}

}
