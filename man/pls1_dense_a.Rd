% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plsR1.R
\name{pls1_dense_a}
\alias{pls1_dense_a}
\alias{pls1_stream_a}
\title{Single-response partial least squares regression (PLS1) another implementation}
\usage{
pls1_dense_a(
  X,
  y,
  ncomp = 2L,
  center = TRUE,
  scale = FALSE,
  tol = 1e-08,
  max_iter = 100L,
  algorithm = c("simpls", "nipals")
)

pls1_stream_a(
  X,
  y,
  ncomp = 2L,
  chunk_size = 1024L,
  center = TRUE,
  scale = FALSE,
  tol = 1e-08,
  algorithm = c("simpls", "nipals")
)
}
\arguments{
\item{X}{A \code{big.matrix} object containing the predictors.}

\item{y}{Either a \code{big.matrix} with a single column or a numeric vector with
the response values.}

\item{ncomp}{Number of latent components to extract.}

\item{center}{Logical; should the predictors and response be centered.}

\item{scale}{Logical; should the predictors and response be scaled to unit
variance before fitting the model.}

\item{tol}{Numerical tolerance used to detect convergence breakdown.}

\item{max_iter}{Maximum number of iterations for the internal solver (kept
for compatibility; the solver adapts automatically when convergence issues
are detected).}

\item{algorithm}{Algorithm used to compute the PLS fit. Either "simpls" or
"nipals". The SIMPLS backend only supports the default centering and
scaling configuration.}

\item{chunk_size}{Number of rows processed at a time by the streaming
backend.}
}
\value{
A list with regression coefficients, intercept, latent scores,
weights and additional metadata.
}
\description{
These helpers wrap high-performance C++ routines built on top of the
\code{bigmemory} and \code{bigalgebra} infrastructure. The \code{pls1_dense_ya}
function performs a standard PLS regression using a NIPALS-style algorithm
without copying the data in memory. The \code{pls1_stream_ya} variant iterates
over the data in blocks which makes it possible to handle out-of-core
datasets efficiently.
}
\examples{
\donttest{
library(bigmemory)
X <- as.big.matrix(matrix(rnorm(2000), nrow = 100))
y <- as.big.matrix(matrix(rnorm(100), ncol = 1))
fit <- pls1_dense_a(X, y, ncomp = 3)
str(fit)
}

\donttest{
library(bigmemory)
X <- as.big.matrix(matrix(rnorm(2000), nrow = 100))
y <- as.big.matrix(matrix(rnorm(100), ncol = 1))
fit <- pls1_stream_a(X, y, ncomp = 3)
str(fit)
}

}
