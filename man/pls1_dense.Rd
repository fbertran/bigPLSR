% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plsR1.R
\name{pls1_dense}
\alias{pls1_dense}
\alias{.harmonize_pls_result}
\alias{pls1_stream}
\title{Single-response partial least squares regression (PLS1)}
\usage{
.harmonize_pls_result(res)

pls1_stream(
  X,
  y,
  ncomp = 2L,
  chunk_size = 1024L,
  center = TRUE,
  scale = FALSE,
  center_y = TRUE,
  scale_y = FALSE,
  algorithm = c("simpls", "nipals")
)
}
\arguments{
\item{X}{A \code{bigmemory::big.matrix} storing the design matrix.}

\item{y}{Numeric vector of responses with length \code{nrow(X)}.}

\item{ncomp}{Number of latent components to compute.}

\item{chunk_size}{Number of rows to process per chunk. Must be strictly
positive. Smaller chunks reduce peak memory usage while larger chunks may
improve speed.}

\item{center}{Should the columns of \code{X} be centered? Defaults to \code{TRUE}.}

\item{scale}{Should the columns of \code{X} be scaled to unit variance? Defaults to \code{FALSE}.}

\item{center_y}{Should the response be centered? Defaults to \code{TRUE}.}

\item{scale_y}{Should the response be scaled to unit variance? Defaults to \code{FALSE}.}

\item{algorithm}{Algorithm to use for the fit. Either "simpls" or "nipals".
When choosing "simpls", preprocessing options must remain at their default
values.}
}
\value{
A list containing regression coefficients, intercept, latent scores,
loadings and weights.
}
\description{
These helpers expose optimised dense and streaming solvers tailored for
partial least squares regression problems where the response consists of a
single column. They wrap the high performance C++ routines shipped with the
package and provide a user friendly entry point when benchmarking the
available implementations.
}
\examples{
\donttest{
library(bigmemory)
X <- as.big.matrix(matrix(rnorm(2000), nrow = 100))
y <- matrix(rnorm(100), ncol = 1)
fit <- pls1_dense(X, y, ncomp = 3)
str(fit)
}

\donttest{
library(bigmemory)
X <- as.big.matrix(matrix(rnorm(2000), nrow = 100))
y <- matrix(rnorm(100), ncol = 1)
fit <- pls1_stream(X, y, ncomp = 3)
str(fit)
}

}
